# ☣ DEAD TACTICS — RULES FOR AI AGENT

> Этот файл читает AI-агент (Cline/Gemini) перед каждым изменением.
> Подключи его в системный промпт Cline: Settings → Custom Instructions → вставь содержимое.

---

## 1. СТРУКТУРА ПРОЕКТА

```
dead-tactics/
├── index.html        ← только HTML-разметка
├── RULES.md         ← этот файл
├── css/
│   ├── styles.css      ← главный (импорты + :root)
│   ├── base.css       ← сброс, body, header
│   ├── game.css       ← грид, клетки, юниты
│   ├── ui.css         ← сайдбар, кнопки, лог
│   ├── components.css  ← карточки, карта, анимации
│   └── screens.css    ← все экраны
└── js/
    ├── config.js      ← константы игры
    ├── constants.js   ← константы рендера и UI
    ├── state.js       ← глобальный стейт
    ├── units.js       ← фабрики юнитов
    ├── helpers.js     ← утилиты
    ├── ui.js          ← UI без логики
    ├── render.js      ← отрисовка
    ├── ai.js          ← AI зомби
    ├── audio.js       ← звуки
    ├── game.js        ← игровая логика
    ├── screens.js     ← навигация
    └── stats.js       ← статистика
```

**Правило:** каждый файл отвечает ровно за одну область. Не смешивай логику и UI.

---

## 2. ЗАПРЕЩЕНО

- ❌ Не удаляй существующие функции — только добавляй или точечно правь
- ❌ Не переписывай файл целиком без явного запроса
- ❌ Не меняй сигнатуры функций (имя, аргументы) без обновления всех вызовов
- ❌ Не добавляй inline-стили в JS — только классы через CSS
- ❌ Не вставляй игровую логику в render.js или ui.js
- ❌ Не дублируй константы — всё в config.js
- ❌ Не используй localStorage, sessionStorage или другие браузерные хранилища (всё в памяти)
- ❌ Не добавляй зависимости (npm, CDN-либы) без явного запроса

---

## 3. ОБЯЗАТЕЛЬНО

- ✅ Перед изменением прочитай затронутый файл целиком
- ✅ При добавлении UI‑элемента используйте классы, не inline-стили
- ✅ Правь только то, что явно указано в задаче
- ✅ После изменения проверь — не сломал ли вызовы в других файлах
- ✅ Новый тип юнита → добавь фабрику в units.js + параметры в config.js
- ✅ Новый экран/оверлей → только в ui.js
- ✅ Новая механика → только в game.js (+ helpers.js если нужна утилита)
- ✅ CSS-переменные (цвета, размеры) только в :root в styles.css

---

## 4. СТЕЙТ ИГРЫ (state.js)

```
units[]       — массив всех юнитов (живых и мёртвых)
selected      — текущий выбранный юнит игрока (null если нет)
phase         — 'placement' | 'player' | 'zombie' | 'over'
placedCount   — сколько юнитов расставлено
turnNum       — номер текущего хода
turnsSurvived — сколько ходов выжило (считается, когда ход передаётся обратно игроку)
highlights    — { move: Set<"x,y">, attack: Set<"x,y"> }
```

**Никогда не создавай отдельный стейт в других файлах.** Всё через эти переменные.

---

## 5. ПАРАМЕТРЫ ЮНИТОВ (config.js)

| Параметр        | Выживший | Зомби |
|----------------|---------|-------|
| hp / maxHp     | 5       | 3     |
| moveRange      | 5       | 3     |
| atkRange       | 4       | 1     |
| weapon         | pistol  | —     |
| atkDmg         | —       | 1     |
| poison (укус)  | —       | +1HP/ход |

Игрок может размещать одновременно не более `leadership` юнитов; это значение хранится в `gameData.player.leadership`. В начале оно равно 1.
Оружие игрока: WEAPONS = { pistol: { baseDmg: 1, critDmg: 2, critChance: 0.10 } }

Менять баланс → только в config.js, не хардкодить числа в логике.

---

## 6. ИГРОВОЙ ЦИКЛ

```
startGame()
  └── phase = 'placement'
        └── игрок расставляет 2 юнита
              └── spawnZombies() → phase = 'player'
                    └── handlePlayer() / btn-end-turn
                          └── phase = 'zombie'
                                └── runZombies() → startPlayerTurn()
                                      └── phase = 'player' (или 'over')
```

---

## 7. СТИЛЬ КОДА

### 7.1 Тестирование

- 🧪 В проекте есть простые unit-тесты в `js/tests/`.
- Запуск: `node js/tests/helpers.test.js` из корня рабочего каталога.
- Тесты должны выполняться без изменений, покрывают утилиты и статистику.
- Перед крупными правками рекомендуется запускать тесты, чтобы не сломать базовую логику.

### 7.2 Стиль

- Язык: JavaScript (vanilla, ES6+), без TypeScript
- Именование: camelCase для функций и переменных, UPPER_CASE для констант
- Комментарии: на русском, кратко, только там где неочевидно
- Функции: максимум 30 строк. Длиннее → разбить
- Отступы: 2 пробела
- Кавычки: одинарные '

---

## 8. ДОБАВЛЕНИЕ НОВОГО КОНТЕНТА

### Новый тип зомби:
1. config.js → добавь ZOMBIE_TYPES.newType = { hp, moveRange, ... }
2. units.js → mkNewZombie(x, y)
3. ai.js → если нужно особое поведение — новая функция zombieNewBehavior()
4. game.js → если нужна спец-механика

### Новый экран (магазин, карта):
1. ui.js → showShop(), hideShop()
2. index.html → добавь overlay-div
3. CSS → добавь стили в соответствующий файл (screens.css или components.css)
4. game.js → логика переходов между экранами

### Новый тип урона/эффекта:
1. state.js → добавь поле в объект юнита
2. game.js → логика применения
3. ui.js → визуальное отображение в сайдбаре

---

## 9. GIT-ДИСЦИПЛИНА

Коммить после каждой рабочей фичи:
```
git add .
git commit -m "feat: добавил магазин"
git commit -m "fix: зомби не двигались через союзников"
git commit -m "balance: снизил урон яда до 0.5/ход"
```

---

## 10. ПУБЛИКАЦИЯ (itch.io)

Когда готово к релизу:
1. Заархивируй папку проекта в .zip
2. itch.io → Create new project → HTML → загрузи .zip
3. Укажи index.html как entry point
4. Бесплатно, без сервера, работает в браузере у любого

---

## 11. НЕПРИКОСНОВЕННОСТЬ БОЕВОЙ СИСТЕМЫ

### 11.1 Перед любыми изменениями — запускай тесты

```bash
node js/tests/helpers.test.js
```

Если тесты падают — **НЕ ТРОГАЙ** код, сначала разберись.

### 11.2 Золотое правило: "Сначала тесты, потом код"

- Перед изменением **любого** файла запусти тесты
- После изменения — снова запусти тесты
- Если сломалось — откати изменения

### 11.3 Регрессионное тестирование

При добавлении **новой фичи** (не багфикс) — вручную проверь:
1. Старт игры → интро → карта → бой
2. Расстановка юнитов работает
3. Ход игрока: движение и атака
4. Ход зомби работает
5. Конец боя и возврат на карту

### 11.4 Чеклист перед коммитом

- [ ] Тесты проходят (`node js/tests/helpers.test.js`)
- [ ] Бой запускается без ошибок в консоли браузера
- [ ] Юниты отображаются на поле
- [ ] Подсветка клеток работает (движение и атака)
- [ ] Рендер вызывается без ошибок

### 11.5 Запрещённые изменения в боевой системе

**НЕ менять сигнатуры** этих функций (без согласования):
- `onCellClick(c, r)`
- `render()`
- `buildGrid()`
- `mkPlayer(x, y)`
- `mkZombie(x, y)`
- `calcDamage(weapon)`
- `unitAt(c, r)`
- `reachable(u)`

### 11.6 Минимальные изменения

че:
1.При любой фи Сделай **одно** минимальное изменение
2. Запусти тесты + проверь бой вручную
3. Если работает — коммит
4. Если сломалось — **откат и разбор**

---

## 12. УНИКАЛЬНОСТЬ ID В DOM

### 12.1 Главное правило
**id должны быть УНИКАЛЬНЫ во всём DOM** - не только в одном файле.

### 12.2 Почему это важно
- `document.getElementById()` ищет во всём document
- SVG pattern с id="grid" конфликтует с div id="grid"
- Это вызывает необъяснимые баги (размеры 0×0, неправильные ссылки)

### 12.3 Запрещённые имена (без префикса)
- grid, box, item, title, container, panel, map, cell
- Все generic имена должны иметь префикс экрана

### 12.4 Правило именования
- HTML id: `<div id="battle-grid">`
- SVG pattern: `<pattern id="map-grid-pattern">`
- Все динамические id в JS должны проверяться через grep

### 12.5 Проверка перед созданием
Перед добавлением нового id:
1. grep по проекту на этот id
2. Если есть совпадение - добавить префикс

### 12.6 CSS классы vs ID
- Для повторяющихся элементов - классы
- id только для уникальных элементов (1 на странице)

---

## 13. NODE.JS И ТЕСТЫ

### 13.1 Все файлы должны экспортировать данные
Если файл используется в тестах - добавь module.exports:
```javascript
// config.js
module.exports = { COLS, ROWS, WEAPONS, ... };

// stats.js
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { recordKill, getStats, ... };
}
```

### 13.2 Глобальные переменные
Минимизируй использование глобальных переменных (units, stats, phase).
Если используешь глобальную переменную в коде - **добавь мок в тесты**:
```javascript
// tests helpers.test.js
global.units = [];
global.COLS = config.COLS;
global.ROWS = config.ROWS;
global.WEAPONS = config.WEAPONS;
global.stats = { zombiesKilled:0, ... };
```

### 13.3 Запуск тестов
```bash
node js/tests/helpers.test.js
```

### 13.4 Перед каждым коммитом
Обязательно запусти тесты - они должны проходить.

---

## 14. СРЕДА РАЗРАБОТКИ

### 14.1 Обязательные инструменты
- **Node.js** - для запуска тестов
- **VS Code** - рекомендуемая IDE
- **Chrome** - для отладки в браузере

### 14.2 VS Code конфиги
Проект включает:
- `.vscode/launch.json` - конфигурации отладки
- `.vscode/tasks.json` - задачи (сервер)

### 14.3 Debug в VS Code
1. Нажми F5
2. Выбери конфигурацию:
   - "Debug: file://" - запуск напрямую
   - "Debug: Run Tests" - unit-тесты
3. Ставь breakpoints в коде

### 14.4 Проверка окружения
Перед началом работы убедись:
```bash
node --version  # должен показать версию
node js/tests/helpers.test.js  # тесты проходят
```

---

## 15. ПОРЯДОК РАБОТЫ

### 15.1 Всегда начинай с коммита

Перед началом работы закоммить текущее рабочее состояние:
```bash
git add .
git commit -m "chore: checkpoint before changes"
```

### 15.2 Один файл — один коммит

Каждое логическое изменение — отдельный коммит:
- `config.js` — отдельно
- `units.js` — отдельно
- `ui.js` — отдельно

### 15.3 Ветки для экспериментов

Для экспериментальных фич создавай ветку:
```bash
git checkout -b feat/shop-system
```

### 15.4 Откат

Если сломалось — откатись:
```bash
git checkout -- .
git status  # проверь что всё чисто
```

---

## 16. ПОСЛЕДОВАТЕЛЬНОСТЬ ВНЕСЕНИЯ ИЗМЕНЕНИЙ

> Перед каждым изменением — строго follow this process:

### 16.1 Анализ и оценка (перед планированием)

**Шаг 1 — Прочитай релевантные файлы:**
- Найди все файлы которые нужно изменить
- Прочитай их полностью перед любыми изменениями

**Шаг 2 — Оцени влияние на проект:**
- Как изменятся параметры, функции, сигнатуры
- Зависимости между модулями
- Что может сломаться

**Шаг 3 — Оценка рисков:**
- Теоретически внеси изменение (на бумаге/в уме)
- Проверь соответствие правилам написания кода
- Проверь что не меняются сигнатуры функций из п. 11.5

### 16.2 Планирование

**Шаг 4 — Детальный план:**
- С учетом сделанного анализа
- Оценка зависимостей
- Прогноз логики работы
- Проработка рисков
- Проверка соответствия RULES

**Шаг 5 — Разбей на минимальные изменения:**
- Одно изменение = один коммит
- Не делай несколько фич в одном коммите

### 16.3 Реализация

**Шаг 6 — Минимальное изменение:**
- Сделай ОДНО минимальное изменение
- Запусти тесты: `node js/tests/helpers.test.js`
- Проверь вручную в браузере

**Шаг 7 — Если работает — коммит:**
```bash
git add .
git commit -m "feat: описание изменения"
```

**Шаг 8 — Если сломалось — откат и разбор:**
```bash
git checkout -- .
git status
```

### 16.4 Чеклист перед каждым коммитом

- [ ] Тесты проходят (`node js/tests/helpers.test.js`)
- [ ] Одно логическое изменение
- [ ] Нет конфликтов id (grep по проекту)
- [ ] Игра запускается без ошибок в консоли браузера
- [ ] Визуальная проверка: юниты, подсветка, UI

### 16.5 Полный цикл работы над задачей

```
1. Проанализируй → 2. Оцени → 3. Спланируй → 4. Реализуй → 5. Тестируй → 6. Коммит
     ↓                  ↓            ↓            ↓            ↓            ↓
  читаю файлы     влияние на     детальный     минимальное    тесты +      git
  и RULES         проект         план          изменение     браузер      commit
```

---

## 17. МИНИМАЛЬНЫЕ ИЗМЕНЕНИЯ

### 17.1 Правило: Одно изменение — один коммит

Каждая задача разбивается на минимальные изменения:
- ❌ Нельзя: "исправил баг + добавил фичу + изменил стили"
- ✅ Можно: отдельные коммиты для каждого

### 17.2 Пример правильного workflow

```bash
# Задача: добавить новый тип зомби
git add js/config.js && git commit -m "config: добавить параметры нового зомби"
git add js/units.js && git commit -m "units: добавить mkNewZombie()"
git add js/ai.js && git commit -m "ai: добавить поведение newZombie"
```

### 17.3 Запрещено

- ❌ Не добавляй несколько файлов в один коммит (если изменения не связаны)
- ❌ Не объединяй разные задачи в один PR
- ❌ Не меняй несколько вещей сразу

---

## 18. СТРУКТУРА CSS

### 18.1 Файлы CSS

```
css/
├── styles.css      ← главный (импорты + :root переменные)
├── base.css       ← сброс, body, header, лейаут
├── game.css       ← грид, клетки, юниты, тултипы, HP-бар
├── ui.css         ← сайдбар, кнопки, лог, оверлей, утилиты
├── components.css ← карточки, карта мира, маркеры, анимации
└── screens.css   ← все экраны (start, map, squad, shop, battle)
```

### 18.2 Правила

- ✅ CSS-переменные (`:root`) — только в styles.css
- ✅ Порядок импорта: base → game → ui → components → screens
- ✅ НЕ дублируй селекторы между файлами
- ✅ Используй классы для повторяющихся элементов
- ✅ ID — только для уникальных элементов (1 на странице)
- ✅ При добавлении стилей — выбери правильный файл по назначению

### 18.3 Куда добавлять стили

| Стили | Файл |
|-------|------|
| Общие теги (body, *) | base.css |
| Игровое поле (#grid, .cell, .unit) | game.css |
| Интерфейс (sidebar, button, #log) | ui.css |
| Карточки (.level-card), карта (.map-container), анимации | components.css |
| Экраны (#screen-start, #screen-map) | screens.css |

### 18.4 Пример добавления стилей экрана

1. Добавь HTML в index.html
2. Добавь CSS в соответствующий файл:
   - Для игровых элементов → game.css
   - Для UI → ui.css  
   - Для нового экрана → screens.css
3. JS использует классы, НЕ меняй CSS без необходимости

---

## 19. ЧЕКЛИСТ ПЕРЕД ЛЮБЫМ КОММИТОМ

- [ ] Тесты проходят (`node js/tests/helpers.test.js`)
- [ ] Нет конфликтов id (grep по проекту)
- [ ] Все зависимости экспортируются (module.exports)
- [ ] Игра запускается без ошибок
- [ ] Визуальная проверка: юниты, подсветка, UI

---

## 20. CONSTANTS.JS

### 20.1 Назначение

Константы рендера и UI вынесены в отдельный файл:
- Размеры SVG карты
- Позиции уровней
- Цвета карты
- Размеры маркеров
- Декоративные элементы

### 20.2 Правила

- ✅ Константы рендера → constants.js
- ✅ Игровые константы → config.js
- ✅ НЕ дублируй между файлами
- ✅ Порядок загрузки: config.js → constants.js → state.js

---

## 21. БАГ: replace_in_file НЕ Работает

### 21.1 Проблема

Инструмент `replace_in_file` может показывать "успех" но не применять изменения. Это известный баг.

### 21.2 Решение

Используй `write_to_file` для полной перезаписи файла.

### 21.3 Проверка

После каждого редактирования проверяй:
- Выполни `git diff` для изменённого файла
- Убедись что изменения реально применились
